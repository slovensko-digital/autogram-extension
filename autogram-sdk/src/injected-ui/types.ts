/**
 * Re-exported from the core types module so headless code (the signing
 * flow) can use it without importing UI modules. Kept here for backwards
 * compatibility of the import path.
 */
export { SigningMethod } from "../types";
